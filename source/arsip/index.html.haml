---
title: Arsip
layout: page
---

- blog.articles.group_by {|y| y.date.year }.each do |year, articles|
  %h3.archive-year= year
  %ul.archive-list
    - articles.each do |article|
      %li
        %strong= link_to article.title, article
        %small 
          %time{datetime: article.date.strftime('%Y-%m-%d')}= article.date.strftime('%e %B %Y')
